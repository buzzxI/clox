#include <stdlib.h>
#include <stdio.h>
#include "common.h"
#include "memory.h"

int main(int argc, const char* argv[]) {
    // void* ptr = malloc(10);
    // char* new_ptr = GROW_ARRAY(char*, ptr, 10, 20);
    // for (int i = 0; i < 20; i++) new_ptr[i] = 'a';
    // new_ptr[20] = 0;
    // fprintf(stdout, "%s\n", new_ptr);
    return 0;
}